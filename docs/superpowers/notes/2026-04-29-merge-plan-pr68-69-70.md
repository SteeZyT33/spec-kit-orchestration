# Merge Plan: PR #68 → #69 → #70 to main

**Date:** 2026-04-29
**Status:** Pre-merge prep complete; awaiting user go-ahead to execute

## State at prep time

| PR | Head → Base | Status | CI | Test count |
|----|-------------|--------|-----|------------|
| #68 | orca-phase-1-rename-strip → main | OPEN, MERGEABLE/CLEAN | all green | (Phase 1 baseline) |
| #69 | orca-phase-2-capability-cores → orca-phase-1-rename-strip | OPEN, MERGEABLE/CLEAN | all green | 154 (Phase 2 baseline) |
| #70 | orca-phase-3-plugin-formats → orca-phase-2-capability-cores | OPEN, MERGEABLE/CLEAN | all green | 462 (Phase 4b prereqs head) |

All three PRs are stacked. CI is green on all three (`Analyze (python)`, `CodeQL`, `CodeRabbit`, `validate`). Branch #70 is 0 behind main and 139 ahead.

## Recommended merge sequence

**Order is mandatory** — each PR's base is the previous PR's head, so they must merge oldest-first.

### Step 1: Merge #68 to main

```bash
gh pr merge 68 --squash --delete-branch
```

- **Method:** squash. Phase 1 is a refactor (rename + kill-list strip); the individual commits aren't load-bearing in main's history. Squash keeps main's log clean.
- **Branch deletion:** safe; Phase 1 is fully captured in #69's base.
- **Post-merge:** GitHub auto-retargets #69 to main (since its old base was just deleted). Verify `gh pr view 69` shows `baseRefName: "main"`.

### Step 2: Merge #69 to main

```bash
gh pr merge 69 --squash --delete-branch
```

- **Method:** squash, same rationale.
- **Post-merge:** GitHub auto-retargets #70 to main. Verify.

### Step 3: Merge #70 to main

```bash
gh pr merge 70 --merge --delete-branch
```

- **Method:** merge commit (NOT squash). Reasoning: #70 contains 6 distinct Phase 4b-pre-N commits + cleanup that are individually meaningful (each cites its prereq number; final batch review verified each). Squashing collapses the prereq distinction and breaks the "Phase 4b spec cites SHA X for Phase 4b-pre-N" pattern that the perf-lab spec PR will use.
- **Alternative:** if squash is the house preference, accept it but include all 6 commit subjects in the squash message body so the prereq trail is preserved.
- **Branch deletion:** safe.

## Pre-merge sanity checks

- [x] All three PRs MERGEABLE/CLEAN
- [x] All three PRs CI green
- [x] PR #70 description updated to reflect what actually shipped (Phase 3 + 3.1 + 3.2 + 4a + 4b prereqs)
- [x] PR #70 description includes the 3-stack merge order
- [x] Test count (462) verified locally
- [x] Phase 4b prereq pre-flight commands verified (`--version`, `contradiction-detector --help`, `build-review-prompt --kind contradiction`)

## Post-merge work (separate commits/PRs after main lands)

### Immediate (within hours of merge)

- **Update local clones / worktrees.** Anyone with a clone of main should `git fetch && git rebase origin/main` their feature branches.
- **Verify the perf-lab spec PR (Plan 2) can cite the new main SHAs.** The plan currently cites pre-merge SHAs (`8478abc`, `bc7af8b`, etc.); after squash-or-merge, the SHAs change. Update Plan 2 with the actual merged SHAs before opening the perf-lab PR.

### Next sprint

- **Brainstorm Spec 015 (brownfield adoption).** The next major track per session-end recommendation. Memory note: "Brownfield adoption gap — 015 spec needed for onboarding existing projects into Orca."
- **Phase 4b perf-lab spec PR (Plan 2).** Independent of Spec 015; can ship anytime.
- **Path-safety consolidation refactor.** Build `orca.core.path_safety` shared module; refactor each capability to use it instead of ad-hoc validation.

### Lower priority

- Update Phase 4a slash commands to use Pre-4 dispatch contract (retroactive stall-detection)
- Release process — semver discipline, CHANGELOG, version-bump workflow
- TUI iteration (deliberately minimal in v1)
- Reconsider PyPI publication if a second external consumer asks (Phase 4b-pre-3 decision artifact has the trigger conditions documented)

## Risks and mitigations

### Risk: CodeRabbit re-runs after auto-retarget

When GitHub auto-retargets #69 (and later #70) to main, CodeRabbit may re-trigger. CI is otherwise stable. Wait for re-runs to complete before clicking merge.

**Mitigation:** monitor `gh pr checks <num>` between each merge step.

### Risk: Conflict from concurrent main commits

Currently 0 commits behind main per `git rev-list --left-right --count origin/main...HEAD`. If someone pushes to main between Step 1 and Step 3, the next PR may need a rebase.

**Mitigation:** run all three merges in one sitting; don't pause for unrelated main pushes.

### Risk: Squash-method changes commit SHAs

The perf-lab spec PR (Plan 2) cites the Phase 4b-pre-N commit SHAs as merge prerequisites. Squash collapses these to one new SHA per stack-PR, breaking the cite. If #70 squashes, the 6 individual prereq SHAs are lost.

**Mitigation:** merge-commit method on #70 (per recommendation above), OR update Plan 2's prereq citations to the post-squash SHAs before opening the perf-lab PR.

## When to execute

User instruction: prep merge but do NOT execute. This document captures the prep state. Resume execution by running Step 1 above when ready.
